# 🔄 인수인계 문서 (HANDOFF)

> 작성: claude.ai 세션 → 터미널 Claude Code 인수
> 작성일: 2026-06-03
> 목적: ABC Frog "단어 퍼즐" 새 기능 개발을 터미널 Claude Code에서 이어가기 위함

---

## 0. 가장 먼저 할 일 (READ FIRST)

1. `abc-frog-project` 스킬이 있으면 먼저 읽어라. 없으면 사용자에게 알리고 노션 "🛠️ 스킬 관리 DB" 백업으로 복구 안내.
2. 작업 폴더는 **반드시** `D:\1Game_projec\ABC_FROG` (이미 원본 repo 존재). 다른 폴더에서 clone 금지.
3. 이 문서는 "지난 대화 요약"일 뿐, **단일 진실의 원천은 코드 + 노션 TODO**다. 의심되면 코드와 노션을 직접 확인하라.

---

## 1. 프로젝트 한 줄 요약

- **이름**: ABC Frog — 3~7세 키즈 영어 교육 게임 (게임 퍼스트, 교육은 덤)
- **컨셉**: 배고픈 개구리가 연잎 위에서 알파벳 든 파리를 혀로 잡아먹기
- **스택**: HTML5 + JS 모듈 구조 (`index.html` + `style.css` + `script.js`)
- **플랫폼**: 웹 → 구글 플레이 출시 중 (현재 v1.0.8까지 배포)

## 2. 저장소 / 경로

| 항목 | 값 |
|------|-----|
| 로컬 작업 폴더 | `D:\1Game_projec\ABC_FROG` |
| GitHub | https://github.com/kimsrain3-rgb/ABC_FROG.git (Public, 토큰 불필요) |
| 현재 브랜치 | `main` |
| 보조 브랜치 | `crown-fix` (왕관 수정 작업) |

### 파일 구조
```
ABC_FROG/
├── index.html   (HTML 뼈대)
├── style.css    (CSS)
├── script.js    (게임 로직)
├── CLAUDE.md    (Claude Code 세션 스킬)
├── assets/images/ (캐릭터/배경 23개)
└── assets/sounds/ (효과음/음성 75개)
```

## 3. 현재 상태 (2026-06-03 기준)

- `main` 브랜치 최신 커밋: `fbe67cd 엔딩 화면 개선: PLAY AGAIN 정중앙 배치 + 왕관 위치 상향`
- **알파벳 잡아먹기 게임은 완성 상태** (3개 모드: ABC / abc / ABc 섞기)
- v1.0.8: GA4 COPPA-safe 분석 + In-App Review API 추가됨
- 즉, 기존 게임은 "출시 가능 완성본". 이제 **새 기능을 얹는 단계**.

## 4. 다음 작업: "단어 퍼즐" (새 기능, 기획 미확정)

- 사용자(꼼지파파)가 ABC Frog **다음 버전으로 단어 퍼즐**을 만들고 싶어 함.
- **아직 방식이 확정되지 않았다.** 사용자와 먼저 방식을 합의할 것. 후보:
  1. **순서대로 글자 잡기** — 'CAT'이 뜨면 C→A→T 순서로 글자 파리를 잡아 단어 완성 (기존 게임 메카닉 재활용, 가장 자연스러움)
  2. **빈칸 채우기** — 'C _ T'에서 빠진 글자(A)를 잡거나 골라 넣기, 그림 힌트 제공
  3. **그림-단어 매칭** — 그림(고양이) 보고 맞는 단어/글자 고르기
- ⚠️ 새 기능은 **노션 기획서 확인 후 범위 합의** → 그다음 구현. 바로 코딩 금지.

## 5. 참고 위치 (단일 진실의 원천)

| 무엇 | 어디 |
|------|------|
| 메인 기획서 + TODO 체크리스트 | 노션 🐸 ABC Frog (page_id: `3097112a-ae24-819e-8d27-f1d38f19258d`) |
| 시리즈 전체 기획 | 노션 🎮 게임 아이디어 (page_id: `3097112a-ae24-819b-b5b5-c3ce269888f0`) |
| 작업 일지 | `C:\Users\USER\.claude\projects\D--1Game-projec\memory\` 의 `project_worklog_*.md` |
| 스킬 백업 | 노션 🛠️ 스킬 관리 DB |

## 6. 작업 규칙 (반드시 지킬 것)

- **수정 전 현재 코드 먼저 확인** — 기억에 의존 금지.
- **한 번에 하나씩** — 여러 기능 동시 변경 금지.
- **에셋 경로 주의** — `assets/images/`, `assets/sounds/`.
- **작업 완료 3종 세트**: ① 코드 수정 → ② GitHub push → ③ 노션 TODO 체크([x]) 까지 반드시 함께.
- **push 가능**: 터미널 버전은 직접 `git push` 가능 (claude.ai 웹은 불가했음).
- **사용자는 코딩 비전공자** — 기술 용어 최소화, 비유로 설명, 결과물 먼저.
- **기획 방향 변경은 반드시 사용자 확인 후**. 버그 수정/코드 정리는 바로 진행 가능.

## 7. 알려진 이슈

- 파리 경계 처리 — 화면 밖으로 나가는 버그 반복 이력.
- 반응형 전환 — PC(1024px+) 풀스크린 / 모바일(768px-) 세로형 분기점 주의.

## 8. 터미널이 첫 턴에 할 일 (체크리스트)

1. [ ] `abc-frog-project` 스킬 로드 (없으면 사용자에게 보고)
2. [ ] `D:\1Game_projec\ABC_FROG`로 이동, `git status` / `git log --oneline -5` 확인
3. [ ] 노션 🐸 ABC Frog 페이지 fetch → TODO 현황 파악
4. [ ] 사용자에게 "단어 퍼즐 방식 3개 후보" 제시 → 방식 합의
5. [ ] 합의 후 기획 정리 → 구현 시작
