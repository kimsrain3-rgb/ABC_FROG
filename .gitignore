index.html.bak

# 서명 키 / 비밀정보 — 절대 커밋 금지 (Public 저장소)
keystore_base64.txt
keystore.properties
**/keystore.properties
*.jks
*.keystore
*.p12
*.env
.env
.claude/settings.json

# === 개발/작업 파일 — 배포 저장소에 안 들어가게 (2026-06) ===
# 자동화 테스트 흔적 (Playwright)
.playwright-mcp/

# 디자인/영상 작업 원본 (무거움) — *.psd 는 어느 폴더든 무시
*.psd
*.prproj
*.prin
# Premiere 작업 폴더 (앱 오디오는 assets/sounds/ 에 따로 있음)
sound/

# 스토어/스케치 자료 (앱 런타임 아님)
assets/SCREEN SHOT/
assets/sketch/
assets/feature-graphic.png

# bkit 도구 상태 파일
docs/.bkit-memory.json
docs/.pdca-status.json
docs/.pdca-snapshots/

# 영상 교체 시 원본 보관 폴더 (2026-08-17) — 내 PC에만 두고 저장소엔 안 올림.
# 원본은 git 태그(backup-before-video-cleanup-20260817)에도 그대로 남아 있어 언제든 되살릴 수 있다.
_backup/

# 앱에 담는 웹 파일 사본 (B-3, 2026-08-27) — 저장소에 두면 같은 파일이 두 벌이 된다(4.3MB).
# twa-project/bake-web-assets.sh 가 빌드 직전에 저장소 원본에서 만들어 넣는다(워크플로에 단계 있음).
# ⭐ 덤: 항상 "그 시점 저장소 원본"이 담기므로 사본이 낡을 일이 없다.
twa-project/app/src/main/assets/web/

# 루트에 흩어진 임시 이미지 (앱 이미지는 assets/ 안에 있음)
/*.png
/*.jpg
/*.jpeg
