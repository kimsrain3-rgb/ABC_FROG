# ABC Frog 프로젝트

## 프로젝트 개요
- **이름**: ABC Frog
- **장르**: 키즈 영어 교육 게임 (게임 퍼스트, 교육은 덤)
- **컨셉**: 배고픈 개구리가 연잎 위에서 알파벳 든 파리를 혀로 잡아먹는 게임
- **타겟**: 3~7세 아이들
- **기술 스택**: HTML5 + JS + CSS (index.html / style.css / script.js 분리, 빌드 도구 없음)
- **플랫폼**: 웹(GitHub Pages) + 안드로이드 WebView 앱 래핑 — **프로덕션 라이브**

## GitHub / 노션
- 저장소: https://github.com/kimsrain3-rgb/ABC_FROG (Public)
- 메인 기획서+에셋: 🐸 ABC Frog (notion page_id: 3097112a-ae24-819e-8d27-f1d38f19258d)
- 시리즈 전체 기획: 🎮 게임 아이디어 (page_id: 3097112a-ae24-819b-b5b5-c3ce269888f0)

### 노션 4개 페이지 구조 (2026-08-21 개편 — 옛 단일 페이지는 은퇴)
> 🐸 ABC Frog 아래 4개로 분리됨. **역할이 다르므로 쓰는 곳을 틀리지 말 것.**
- 📍 **현재 상태** (`3c37112a-ae24-8167-b13e-e712434c7a13`) — **세션 시작 시 이것만 읽는다.** 쌓지 않고 **덮어쓰기**
- 📒 **작업 로그 2026-08** (`3c37112a-ae24-8122-85ae-e833111d46d1`) — 그날 한 일. **맨 위에 추가.** ⚠️ **달이 바뀌면 새 페이지**(2026-09)를 만든다. 한 페이지 2만 자 넘으면 자른다
- 📖 **결정 기록** (`3c37112a-ae24-8168-9db7-c3b5e6b33204`) — "왜 그렇게 정했나"(시간순으로 안 흐르는 것)
- 📌 **진행 중 과제 목록** (`3bc7112a-ae24-8112-8747-dd9afc4dcdb0`) — 항목별 전말·근거
- 🗄️ 옛 작업 로그 (`39c7112a-ae24-81d6-84e2-c4f46b9f8c70`) — **2026-08-20 이전. 여기 새로 쓰지 말 것.** 12만 자라 통째로 읽지 말고 검색만

## 현재 상태 (요약)
- ✅ **프로덕션 라이브** — versionCode 10 / 1.1.0 (targetSdk 36) · 실유저 300명+ · 2026-08-06 기준 **프로덕션 100% 출시 완료**
- ✅ 게임: 파리잡기(ABC/abc/ABc) + 단어퍼즐 3종(과일·동물·공룡) + 엔딩 영상·음성
- ✅ 연출: 개구리 반응 클립 + 글자 반짝 + 온보딩 · 시작화면 퍼즐 선택 메뉴 개선
- 🩺 **전역 에러 추적 가동** — `error-tracker.js`가 GA4 `game_error` 전송(script/video/timeout). ※ **웹 파일이라 앱 업데이트와 무관하게 2026-07-28부터 이미 동작 중**이었음(vc10 버전 이름만 "에러 추적"이었던 것)
- ✅ **물리 뒤로가기 수정 완료 — vc10 출시로 실유저 반영**(2026-08-06) — 예측형 뒤로가기(targetSdk 35+ 기본 ON)가 `onKeyDown`을 안 불러 우리 처리가 통째 무시되고 있었음. `OnBackInvokedCallback` 등록으로 해결. **vc10이 앱에 실제로 새로 넣은 변경은 이것**(에러추적은 웹 쪽). 자세한 원인은 CHANGELOG 2026-07-30 참고
- 🖼️ **이미지 축소 완료** — 개구리 2953²→768²(시작화면용 4a·4b만 1024²)·사과·연잎·배경압축. 첫 화면 9.21→3.20MB, 디코딩 RAM 700→54MB (저사양 기기 멈춤 대응). 원본=`assets/_original/`
- 🍎🔠 **과일 퍼즐 그림 다이어트 + 완성 단어 글자 키우기 라이브 완료**(2026-08-20, 커밋 `0ad43f7`(그림)·`7f0e982`(글자)) — ①**그림**: 과일 10장을 원본 해상도 그대로 두고 **WebP 손실 q80·method6** 으로만 줄임. **16.4→0.77MB(−95.3%, 14.87MB 절약)**. ⚠️ **PNG 256색은 쓰지 말 것** — 팔레트 변환이 반투명 가장자리를 뭉개 **투명 픽셀 수가 바뀌고**, 퍼즐 조각 마스크는 그림의 **알파(`alpha>20`)에서만** 만들어지므로 조각이 어긋난다. WebP 손실은 알파를 무손실로 넣어 **비트 단위로 안 변한다**(10장 실측 최대차 0). ⚠️ 확장자가 바뀌어 주소가 달라지므로 `?v=` 갱신 불필요. 원본 PNG 는 안 지운다. ②**글자**: 완성 단어 크기 기준을 퍼즐판(`board`)→**화면 폭**, 공식→**실측**(`wpFitWord`, `dino.html` 의 `fitWord()` 방식). 상한 `board*0.2*fscale` 을 그대로 둬 **짧은 단어는 안 변하고 긴 단어만 커진다**(STRAWBERRY ×1.20). ⚠️ 폭은 **`offsetWidth`** 로 잴 것 — `getBoundingClientRect()` 는 `shout` 의 1.27배 확대가 포함돼 글자를 엉뚱하게 줄인다. ★ 덤으로 **폰 글자확대 130%↑에서 단어가 잘리던 문제**도 막혔다. 자세한 건 CHANGELOG 2026-08-20
- 🔤🚀 **파닉스 satpin 라이브 공개 완료**(2026-08-12, 커밋 `0515f37`, 백업태그 `backup-before-phonics-live-20260812`) — 시작화면 Phonics 카드 잠금 해제 → `phonics/index.html` 전체화면 오버레이(동물·공룡 퍼즐과 같은 구조). 단어 6종 `sit·pat·nap·pan·sip·tap`. ⚠️ **`VID_VER`(영상 캐시 날짜)가 이제 `phonics/`(라이브)·`test/phonics/`(테스트) 두 곳에 있다 — 영상 갈아끼우면 둘 다 갱신.** 새 단어는 단어별 **통발음(`word_*.mp3`)+보상 영상 3편**이 추가로 필요. 자세한 건 CHANGELOG 2026-08-12
- ✅ **"로딩 중 버튼 먹통" 수정 라이브 반영 완료**(2026-08-05, 커밋 `af4261a`, 백업태그 `backup-before-bootguard-20260805`) — 시작화면 버튼은 HTML만으로 즉시 눌리는데 처리 함수(인라인 onclick 11개)는 body 끝 `script.js` 도착 후에야 생김 → 느린 네트워크(멕시코·필리핀·네팔)에서 `ReferenceError`. `window.onerror`가 삼켜서 **조용히 무반응**이었음. `index.html` head에 '대기표' 함수를 두고 눌린 걸 기억했다가 `__bootFlush()`로 자동 실행(83줄 순수 추가). **효과 확인 = 며칠 뒤 GA4 `ReferenceError` 건수 감소 여부**(`init_timeout`은 로딩을 안 건드렸으므로 남는 게 정상). 자세한 건 CHANGELOG 2026-08-05
- 📉 **GA4 급락(7/29~) 원인 미확정** — 에러추적기는 무혐의 확정(전체의 1.6%). `pushState` 가짜 page_view는 확인됨. 남은 모순: **세션 없이 사용자만 계수됨**(세션 61 vs 사용자 추정 226, first_visit 19). 같은 기간 활성 사용자 실수치 + 7/24~7/27 기준선 필요
- ✅ **시작화면 3버튼(ABC/Word/Phonics) + 퍼즐메뉴 여백 라이브 완료**(2026-08-05, 커밋 `a7cea37`, 백업태그 `backup-before-phonicsbtn-20260805`) — 시작화면 카드를 Word 메뉴와 같은 **가로 알약 244px 3장 세로 배치**로 변경. Phonics는 잠긴 카드(`wcLocked` 재사용, 아이콘 🗣️). 퍼즐메뉴 Insect 아래 여백도 함께 해소(짧은 화면 단계별 축소). ⚠️ `#wcToast`를 `.wc`→`#gc` 직속으로 옮김(시작화면에서도 쓰려고). 자세한 건 CHANGELOG 2026-08-05
- 🗣️ **파닉스 세트 선택 메뉴 라이브 완료**(2026-08-13, 커밋 `37b3019`, 백업태그 `backup-before-phonicsmenu-20260813`) — Phonics 버튼 → **세트 메뉴(카드 4장)** → Phonics 1 = satpin 게임. 2·3·4는 잠금(`wcLocked` 재사용). ⚠️ **메뉴 화면(`#ps`)은 `index.html` 이 아니라 `script.js` 의 `_buildPhonicsMenu()` 가 만든다** — `index.html` 은 캐시버스터가 없어 옛 판이 폰에 남기 때문(2026-08-12 파닉스 잠김과 같은 함정). 새 세트를 열려면 `PH_SETS` 의 `open:true` + `goPhonicsSet()` 분기만 고치면 된다. ⚠️ **그때 GA4 에 세트 구분(`set`)도 함께 넣을 것** — 지금은 세트가 1개뿐이라 `word` 로 구분되지만, 2개 이상이면 어느 세트인지 알 수 없어진다("측정 없이 내보내지 않는다"). 자세한 건 CHANGELOG 2026-08-13
- 🪶 **첫 화면 로딩 다이어트 라이브 완료**(2026-08-14, 커밋 `197bf55`, 백업태그 `backup-before-lazyimg-20260814`) — 첫 화면 **6,863→4,329KB(−37%)**, 그림만 −75%. ①Word 버튼 아이콘 974KB→21KB(`fruit_apple_icon.png` 신규. **퍼즐용 원본은 그대로**) ②개구리 18장 `src`→`data-src` 로 미루고 `preloadFrogImgs()` 가 게임 진입 때 2장씩 받음. ⚠️ **`loading="lazy"` 로 바꾸지 말 것** — 전부 `display:none` 이라 영영 안 받는다. `fs1a`·`fs4a` 는 일부러 `src` 유지(각각 게임 시작 단계 / `frog-reactions.js` 발위치 계산용). 자세한 건 CHANGELOG 2026-08-14
- 🪵 **ABc 나무둥치 위치 라이브 완료**(2026-08-14, 커밋 `1a53ed8`, 백업태그 `backup-before-stump7-20260814`) — 폰만 `.stump{bottom:5%→7%}`. ⚠️ **PC 값(-2%)은 일부러 안 바꿨다** — 크기는 `vmin`, 위치는 화면높이 `%` 라 PC·폰에 같은 %를 더하면 안 된다(PC는 0%로 올리니 개구리가 파묻힘). `style.css` 주석 참고
- 🔇 **소리 지연 로딩 라이브 완료**(2026-08-15, 백업태그 `backup-before-soundlazy-20260815`) — 첫 화면 소리 **3,271→49KB(−98%)**. 첫 화면은 원래 **완전 무음**인데 소리 49개를 다 받고 있었다(옛 `script.js` 43~46·327~377줄이 최상위 `const`). → `SND_SRC`/`VOICE_SRC` 에 **주소만** 두고 `sndOf()`/`voiceOf()` 가 필요할 때 생성(재사용). 1단계=게임 진입 즉시 4개, 2단계=1.2초 뒤 45개. `bgm.mp3` 1,413→740KB 교체(`?v=20260815`). ⚠️ **소리를 끄는 자리엔 반드시 `sndMade()`** — `sndOf()` 는 없으면 만들어버려 *끄려다 오히려 파일을 받는다.* ⚠️ 새 소리는 최상위에서 `safeAudio()` 부르지 말고 목록에 주소만 추가할 것. 자세한 건 CHANGELOG 2026-08-15
- 🎬 **C-2 적응형 보상 영상 완료(공룡·동물·파닉스 38편)**(2026-08-17~18, 마지막 커밋 `5ee0c8b`, 백업태그 `backup-before-adaptive-video-20260817`) — 회선에 맞춰 원본/650KB/400KB 를 자동으로 고른다. **2g 기준 76.3→14.2MB(−81%)**. 공용 파일 `video-quality.js` 의 `pick()`·`original()`·`reportFallback()` 을 4개 문서가 함께 쓴다. ⚠️ **주소 조립 순서 = `pick()` 먼저, 캐시 꼬리표(`bust()`/`VID_VER`) 나중.** ⚠️ 새 보상 영상을 추가하면 **티어 2개도 같이 만들어야** 폴백으로 원본이 나가 무거워진다. 비상 스위치 = `FORCE_ORIGINAL=true`. 파닉스는 소리 트랙 없음(`-an`, 어차피 muted). ✅ **GA4 맞춤 정의는 전부 등록 완료**(2026-08-21 노션 확인) — C-2 5개(`tier`·`reason`·`effective_type`·`save_data`·`puzzle`)도, C-3 7개(`category`·`word`·`result` / `ready_ms`·`wait_ms`·`lead_ms`·`solve_ms`)도 이미 6/30·8/18에 등록돼 있었다. 자세한 건 CHANGELOG 2026-08-17·08-18
- 🎬 **C-3 미리받기 0단계 라이브(관찰만)**(2026-08-20, 커밋 `363f522`, 백업태그 `backup-before-prefetch-20260820`) — `video-prefetch.js` 신규(`warm`/`ready`/`cancel`/`PREFETCH_ON`). **미리받기는 아직 안 켰다(`PREFETCH_ON=false`)** — 며칠 돌려 '전' 기준선(`video_prefetch result=off` 의 `ready_ms`)을 쌓는 중. `solve_ms`(퍼즐 소요시간)를 공룡·동물·과일에 추가(파닉스는 기존). ⚠️ **`ready()` 가 이 작업의 핵심** — 받는 중에 재생이 시작되면 브라우저가 처음부터 다시 받는다(실측 2.4MB→4.06MB). ⚠️ **`warm()`/`ready()` 는 주소를 스스로 조립하지 않는다** — 부르는 쪽이 `pick()` 거친 재생용 주소를 그대로 넘긴다(원본+티어 이중 수신 방지, 어긋나면 `result=mismatch` 자동 신고). 다음 = 1단계 공룡, **`test/` → 폰 확인 → 라이브**. 자세한 건 `docs/C-3-plan.md` + CHANGELOG 2026-08-20
- 🎬 **엔딩 축하 영상 라이브 완료**(2026-08-25, 백업태그 `backup-before-endvideo-20260825`) — 26글자를 다 모으면 `frog_lastdance.mp4`(8초·음악)를 화면 가득 튼다. 시간표를 압축해 **마지막 파리 → PLAY AGAIN 이 정상 9.5초 / 영상 실패 3.6초**(예전 11초+). ⚠️ **테스트판(`test/current-ending-video.html`)의 엔딩 블록 500줄은 지웠다** — 라이브에 같은 기능이 있어 안 지우면 영상이 두 개 뜬다. 남은 건 🏁 26글자 버튼뿐이라 이제 그 판 = 라이브 그대로. ⚠️ **`index.html` 이 `video-quality.js` 를 안 읽고 있었다**(CLAUDE.md 기록과 달랐음) → 추가함. 그래서 **GA4 `video_tier` 가 이제 모든 세션에서 발사**된다(예전엔 퍼즐 연 세션만). ⚠️ 폭죽은 **영상이 뜨면 전부 버리고, 안 뜨면 810개 그대로 다 터뜨린다**(`fwPlace()` 문지기 — 폰에서 폭죽이 영상을 끊었다). ⚠️ 새 영상 넣으면 티어 2개(`videos_650/`·`videos_400/`)도 같이 만들 것. 자세한 건 CHANGELOG 2026-08-25
- 🔜 향후: **첫 화면 마지막 소리 48.6KB**(`frog-reactions.js:409` `ensureSnd()` 가 `alphabet_sparkle.mp3` 를 `preload='auto'` 로 즉시 받음 — 재생은 정답 잡을 때만) / 공룡 2단계 "먹이주기" / 곤충(Insect) 퍼즐
- 🐸 **시작화면 인사(손흔들기) 끄기 — 라이브 반영 완료**(2026-08-10, 커밋 `fb1b4a5`, 백업태그 `backup-before-nogreet-20260810`). 첫 화면엔 앉은 개구리만, 액션 클립 5종은 게임 안에서 그대로. `frog-reactions.js`의 `GREET_ON=false` → `true` 한 글자면 복원. 첫 화면 460KB webm 다운로드도 함께 사라짐. ※ 인사는 2026-07-21 `a8dfd11`로 들어간 **의도된 기능**이었고, "나올 때·안 나올 때"가 갈린 주원인은 **앱 완전종료 후 켜기 vs 홈에서 복귀**(MainActivity `loadUrl`이 `onCreate`에만 있음). 자세한 건 CHANGELOG 2026-08-10
- 📜 **완료 이력 전체 → `docs/CHANGELOG.md`** (커밋번호·백업태그·기술 세부·되돌리는 법 전부 거기. 비상 복구 시 CHANGELOG에서 해당 날짜의 백업태그를 찾을 것)
- ⚠️ **앱은 WebView로 GitHub Pages 실시간 로딩 → `main` push = 즉시 전 유저 반영. 푸시 전 반드시 검증.**

## 현재 파일 구조 (핵심만)
```
ABC_FROG/
├── index.html          ← HTML 뼈대 (+ 캐시버스터 always-fresh 로더)
├── style.css           ← CSS
├── script.js           ← 게임 로직 (파리잡기 + 과일 단어퍼즐 + 퍼즐 오버레이 호출)
├── frog-reactions.js   ← 개구리 반응·글자 반짝·온보딩 애드온 (index.html이 script.js 뒤에 로드)
├── error-tracker.js    ← 전역 에러 추적 → GA4 game_error (head에서 script.js보다 먼저 로드. 퍼즐·파닉스 페이지도 공용)
├── animal.html         ← 동물 퍼즐 (iframe 오버레이, 게임 코드와 분리)
├── dino.html           ← 공룡 퍼즐 (iframe 오버레이, Animal과 동일 방식)
├── manifest.json / service-worker.js(킬스위치) / privacy-policy.html
├── assets/             ← 주제별 폴더: frog · game · fruit · animal · dino · abc · phonics · ui · fonts …
├── test/               ← 폰 테스트 전용 (고정주소 /test/, 아래 "테스트 방법" 참고)
├── twa-project/        ← 안드로이드 순수 WebView 앱 (Gradle, MainActivity.java)
├── .github/workflows/  ← GitHub Actions (build-aab.yml)
└── docs/               ← CHANGELOG.md 등
```
※ `data-word-fruits.js`(과일 데이터 분리)는 **main에 없음** — `refactor/split-fruit-data` 브랜치에만 있음 (TODO 참고)

## 게임 디자인 (참고)
- **핵심 메카닉**: 정답 파리 → "야미야미!" + 알파벳 발음 + 별 + 배 빵빵 / 오답 → "우웩!" + 토하기 / 콤보(야미→야미야미→…) / 개구리 감정 4종(hungry·yummy·yucky·full)
- **캐릭터**: 개구리(메인, 스프라이트 애니), 나비(4프레임), 애벌레(9프레임), 파리(알파벳 타겟)
- **ABC 노래 엔딩 컨셉**: 파리 합창단 + 개구리 지휘자, 호명 시 볼록렌즈 + 반짝임

## 에셋 관리 방침
- 그림/소리는 `assets/`에 실제 파일로 (base64 내장 X)
- 그림 추가/교체 빈번 예정 (러프스케치 → 깔끔한 라인 업그레이드 등)
- 파일명 알아보기 쉽게: `frog_hungry.png`, `fly.png` 등
- ⚠️ **기존 파일을 같은 이름으로 교체하면 캐시됨** → 교체 시 파일명 변경 또는 `?v=` 갱신 (아래 배포/캐시 규칙)
- 🔊 **소리를 "더 크게" 요청받으면 볼륨 숫자부터 올리지 말 것** — 기존 소리 파일은 피크가 천장에 붙어 있는 게 많음(`letter_*.mp3` 실측 0.0~-0.4dB). 1.0 초과 게인은 곧장 깨짐이고 `<audio>.volume`은 애초에 상한 1.0. 더 키우려면 **WebAudio 게인 + 리미터**(`script.js`의 `wpLetterOut()` 패턴, 2026-07-29). 음량 비교는 LUFS 말고 **RMS**(`ffmpeg -af volumedetect`의 mean_volume) — 1초 미만 짧은 소리에서 LUFS는 부정확
- 🖼️ **그림 픽셀 크기 상한 = 화면 표시 크기 × 3** (QHD폰 배율 기준). PNG는 화면에 뜨는 순간 `가로×세로×4byte`를 RAM에 차지 → 2953²=장당 35MB로 저사양 기기가 죽음(2026-07-28). 새 그림은 **CSS 표시 크기부터 계산하고** 넣을 것: `vmin/vw`=화면폭 비율(배율 무관), 고정 `px`=값×폰배율(2.6~4.0)

## Google Play 배포 정보
- **패키지명**: com.ggomzipapa.abcfrog
- **현재 버전**: versionCode 10 / 1.1.0 (프로덕션 100% 출시, targetSdk 36, 2026-08-06 확인) — 앱 쪽 변경 = 물리 뒤로가기(`OnBackInvokedCallback`) 수정
- **이전 버전**: versionCode 9 / 1.0.9 (2026-07-27 심사 통과)
- **다음 버전**: **versionCode 11 / 1.1.1 — 2026-08-13 심사 제출 완료, 결과 대기 중**(커밋 `03da907`). 내용 3건 = ①**최신 반영**(`onResume`에서 30분↑ 이탈 + 시작화면일 때만 `reload`. `loadUrl`이 `onCreate`에만 있어 백그라운드 복귀 시 옛 화면이 남던 문제 — 2026-08-10 인사 클립·2026-08-12 파닉스 잠김의 공통 원인) ②**오프라인 안내화면**(`onReceivedError` + `isForMainFrame()` 검사 필수) ③`setTextZoom(100)`. ⚠️ 프리런치 보고서 확인 전에 제출됨 + 실기기 미검증 코드 → 보고서 확인 필수
- **서명키 SHA1**: D7:D4:13:7D:B1:44:7D:00:35:0F:1C:CD:26:18:90:DB:7B:87:68:29
- **서명키 SHA256**: 6F:DE:2D:08:2E:33:E0:B8:C9:E4:20:E4:D2:08:68:41:AC:2F:27:27:53:23:3F:EC:FB:B0:7D:CB:67:06:95:54
- **서명키 위치**: GitHub Secret `KEYSTORE_BASE64` + 로컬 백업 (`/d/1Game_projec/AAB, AAB_KEY/`)
- **개발자 계정 ID**: 8921467846864051720
- **개인정보처리방침**: https://kimsrain3-rgb.github.io/ABC_FROG/privacy-policy.html
- **연락처**: ccomzpapa@naver.com
- **assetlinks.json**: .well-known/assetlinks.json (GitHub Pages 서빙)

## 알려진 이슈
- 파리 경계 처리 — 화면 밖으로 나가는 버그 반복 발생 이력
- 반응형 전환 — PC(1024px+)/모바일(768px-) 분기점 주의

## 미처리 TODO (미완료만 — 완료분은 docs/CHANGELOG.md)
> ⚠️ **기능이 완료/라이브되면 그 자리에서 이 목록의 해당 줄을 지울 것.** (2026-06-26 동물 퍼즐 통합 후 줄이 남아 나중에 "미통합"으로 잘못 보고된 적 있음 — 2026-07-27 정리)
> ⚠️ **화면 전환 판정 규칙**: `.wc`/`.ms`/`.wp`는 `.ss`를 **덮기만** 하고 `display:none`으로 안 만듦 → "시작화면 벗어남"을 `.ss`의 display로 판정하지 말 것. `frog-reactions.js`의 `otherScreenOpen()`(ms/wc/wp + 동물·공룡 iframe 검사)을 쓸 것. (2026-07-28 인사 영상 겹침 버그의 원인)
- [ ] 🟡 나비/애벌레 혀(`isShooting`)와 게임 혀(`ia`) 잠금변수 달라 동시탭 시 혀 겹침 글리치
- [ ] 🟢 파리 먹힐 때 입 위치 고정값(0.5,0.38) — 4~5단계 뚱뚱 개구리 입(0.56)과 어긋남
- [ ] 🟢 `wpSayWord`가 `new Audio()` 직접 사용 → `safeAudio()`로 통일 권장 (위험 낮음)
- [ ] 🟢 `buildPuzzle` 비동기 race(잠재): `_im.onload`가 전역 `wpCurrent` 참조 → 빌드 겹치면 엉뚱한 과일. 실플레이 미발생, 우선순위 낮음. (※ 퍼즐이 '깨진 이미지'면 거의 항상 **로컬 테스트서버 꺼짐**이 원인, 코드버그 아님)
- [ ] 🟡📊 **공룡·동물도 완성만 알고 중간을 모름** — 부모가 받는 신호가 `*_done` 하나뿐이라 **다음 퍼즐 시작·다시하기를 알 수 없다**(`script.js:1882` 주석 참고. 그래서 뒤로 버튼 밝기를 0.5초 폴링으로 때우고 있다). 별도 과제
- [ ] 🔴📦 **vc11 프리런치 보고서 확인** — 2026-08-13 **심사 제출 완료(검토 시작까지 누름)**, 결과 대기 중. 프리런치 보고서는 심사와 별개로 생성되므로 **나오는 대로 크래시·ANR 확인**할 것. 크래시 있으면 롤아웃 중지 또는 vc12 준비. 통과 후 실폰 3건 확인(30분 후 복귀 시 갱신 / 비행기모드 안내화면 / 글자크기 최대에서 공룡 이름)
- [ ] 🔴🔐 **서명키 비밀번호 교체**: `twa-project/app/build.gradle`에 `abcfrog123` 평문 노출(깃 히스토리 포함). keytool로 비번 변경 → Secret 재등록 → build.gradle 평문 제거(환경변수 참조) → AAB 재빌드. (서명키 자체는 교체 불가, 비번만)
- [ ] 🔵 refactor 브랜치(`data-word-fruits.js` 데이터 분리) main 병합 — 검증 완료, always-fresh 방식과 정합 확인 후 머지
- [ ] 🟡🎨 **동물 퍼즐 너구리 — 에셋 재작업 필요 (366×652 → 최소 1236×2214)**. ⚠️ **용량 문제가 아니다**(2026-08-19 정정). 화면에는 412×734로 표시되는데 원본이 366×652라 **확대되어 흐리다**(다른 동물은 941×1672). 알파 채널도 있지만 **투명 픽셀이 0개**라 아무 일도 안 한다. 압축·JPG화로는 해결되지 않는다 — **사장님이 새로 그려 주셔야 한다.** (옛 기록: "JPG화로 용량↓"는 틀린 진단이었음)
  - ※ 동물 퍼즐 시즌1 10종은 **2026-06-26 본게임 통합·실유저 공개 완료**(커밋 `7c9a46b`) — 완료분이라 여기 아닌 CHANGELOG 참조.

## 협업 규칙

### 역할
- **꼼지파파** = 아이디어 + 지시 + 에셋(그림/소리) + 최종 판단
- **Claude** = 실무(코드/구현/파일정리)
- 꼼지파파는 코딩 비전공자 — 기술 용어 최소화, 비유로 설명할 것

### 코드 수정 원칙
- 수정 전 반드시 현재 코드 확인 — 기억에 의존하지 말 것
- 한 번에 하나씩 — 여러 기능을 동시에 바꾸지 말 것
- 변수명/함수명 바꿀 때 참조하는 곳 모두 확인
- 파일 분리/수정 후 게임 기능이 100% 동일하게 작동해야 함

### 측정 없이 내보내지 않는다 (필수 준수)
> 파닉스는 2026-08-12 공개 후 **완성 이벤트가 없어 완주율을 하나도 모른다.** C-2도 영상 티어를 다 만든 뒤에야 "어느 게 나갔는지 모른다"를 깨닫고 GA4를 나중에 붙였다. **둘 다 기능은 됐는데 효과를 잴 수 없었고, 늦게 붙이느라 그전 데이터를 영영 못 얻었다.**
- 새 게임·퍼즐·콘텐츠를 라이브에 내보낼 때는 최소한 **"열었다"와 "끝냈다"**를 GA4로 보내는 코드를 **같이** 넣는다
- **표준 방식**: 자식 문서에서 `postMessage` → `script.js`가 받아 `gtag('word_puzzle_complete',{category:'...'})` 발사. (`dino.html:480` + `script.js:1602`가 표준 예시. animal도 동일)
- **`category` 이름을 기존과 나란히 붙일 것.** GA4에서 비교가 되어야 의미가 있다
- 새 매개변수를 추가하면 **GA4 맞춤 측정기준 등록이 필요하다(사장님 몫)** → 매개변수 이름을 확정해 **목록으로 보고할 것.** 등록 전 데이터는 보고서에서 안 쪼개진다

### 테스트 방법 (필수 — 다른 창/세션에서도 이대로)
- **모든 게임 테스트는 고정 주소 하나로 한다**: https://kimsrain3-rgb.github.io/ABC_FROG/test/
- 폰에서 **"홈 화면에 추가"** 해서 앱처럼(전체화면)으로 테스트한다. (브라우저 탭은 주소창 때문에 작아 보임 → 크기가 라이브와 다르게 보이는 것 방지)
- **새 게임(동물·공룡 등)도 이 같은 주소에 내용만 갈아끼운다.** 새 테스트 주소 만들지 말 것.
- **시크릿 탭**이나 **매번 바뀌는 긴 githack 주소** 방식은 쓰지 말 것. (githack은 전체게임에서 "Open the page" 경고+캐시로 깨짐)
- 구현(Claude용): 테스트 환경 = main의 `test/` 폴더(`test/index.html` + `test/script.js`). 후보 코드를 `test/script.js`에 넣고 main에 push → Pages 자동배포. assets/style.css는 라이브 공유(`<base href="../">`), 캐시버스터+서비스워커 자동정리로 항상 최신. 라이브 루트(`/index.html`,`/script.js`)는 안 건드림. 검증 후 진짜 `script.js`에 반영해 라이브 배포.
- ⚠️ **테스트 앱(크롬)의 사각지대**: 본게임 앱(WebView)은 폰 '글자 크기' 설정을 웹 글자에 곱하지만 크롬은 안 곱함(2026-07-10 공룡 이름 잘림). → **글자/레이아웃 수정은** ① 검증 시 글자확대 110~130% 조건 포함(Playwright로 재현 가능), ② 라이브 반영 후 **본게임 앱에서도 최종 확인**. (근본 차단 = MainActivity `setTextZoom(100)`, TODO 참고)

### Google Play 제출 규칙 (필수 준수)
**절대 "검토 시작"을 바로 누르지 말 것!** 반드시 프리 런치 보고서를 먼저 확인한다.
1. AAB 빌드 (GitHub Actions → Build AAB)
2. Play Console → 비공개 테스트 → AAB 업로드
3. **"저장"만** 누르기 (❌ "검토 시작" 누르지 말기)
4. **사전 출시 보고서(프리 런치 보고서)** 결과 확인 (몇 시간~하루)
5. 크래시 없음 확인 → **그때 "검토 시작"** 누르기
6. 크래시 있으면 → 로그 확인 → 수정 → 1번부터 다시

### Google Play 안정성 규칙 (WebView 앱 — 필수 준수)
순수 WebView 앱이라 **JS 에러 = 앱 크래시 = 심사 거부.** 방어적 프로그래밍 필수.
1. **전역 에러 핸들러 유지** — `window.onerror` + `unhandledrejection`(script.js 최상단). 절대 제거 금지
2. **Audio는 반드시 `safeAudio()` 사용** — `new Audio()` 직접 호출 금지 (실패 시 더미 객체 반환 래퍼)
3. **SpeechSynthesis는 try-catch 필수** — `sp()` 안에서 처리. TTS 없는 기기 크래시 방지
4. **AudioContext 사용 전 null 체크** — `ea()` 후 `if(!ax) return;` 패턴 유지 (`pt`,`psg`,`pbr`)
5. **게임 루프(`gl`) try-catch 유지** — `uf()` 호출 감싸 한 프레임 에러가 전체 루프 안 멈추게
6. **브라우저 API 사용 전 존재 확인** — `'speechSynthesis' in window`, `window.AudioContext||window.webkitAudioContext` 등
7. **새 기능 추가 시** — `.play()`,`.speak()`,DOM API 등 실패 가능한 호출은 항상 `.catch(()=>{})` 또는 try-catch
8. **화면 폭에 꽉 차는 글자는 공식(글자수×추정폭)으로 크기 정하지 말 것** — 실제 렌더 폭을 재서 줄이는 방식(`dino.html`의 `fitWord()` 패턴). WebView 글자확대·폰트 폴백·좁은 폰에서 공식은 반드시 어긋남. 확대 애니메이션이 있으면 **최고점 배율 실측**(wpWordShout는 명시 1.25지만 오버슛 포함 1.27) 기준으로 여유 확보

### 배포 / 캐시 규칙 (필수 준수)
> 앱이 WebView(`setCacheMode(LOAD_DEFAULT)`)로 GitHub Pages를 로드 → 캐시 때문에 "수정했는데 폰엔 옛 코드"가 반복됐던 영역. **방법이 아니라 목적을 지킬 것.**
1. **목적(불변)**: 푸시하면 **유저가 반드시 최신을 받아야 한다.** 현재 방법 = `index.html`이 `document.write`로 `script.js?b=Date.now()`/`style.css?b=Date.now()` 붙여 **매 실행 always-fresh** 로드. **이 로더를 정적 `?v=`로 되돌리지 말 것.** 더 나은 방법으로 바꾸려면 새 방법도 "푸시 후 최신 보장" + **실제 폰 검증** 통과해야 함.
2. **그림·소리 교체 주의**: always-fresh는 **코드(js/css)만** 커버. 이미지/사운드를 **같은 파일명으로 교체**하면 여전히 캐시됨 → 파일명 변경 또는 `?v=` 갱신. (새 파일명 추가는 OK)
3. **검증은 반드시 실제 폰에서**: 캐시는 여러 겹 — "한 겹 막았다 = 다 막았다" 아님. 로컬 강력새로고침은 캐시를 숨김 → 최종 검증은 **실제 폰 앱 완전종료→재시작** 후. (※ 오래 깔린 기기는 캐시/옛 서비스워커로 며칠 늦게 반영될 수 있음. 즉시 100% 보장하려면 MainActivity에 캐시 무력화 넣어 Play 업데이트 — 1회성)

### 세션 시작 프로토콜
1. 이 CLAUDE.md 읽기 (자동)
2. **노션 📍 현재 상태 페이지 읽기** (`3c37112a-ae24-8167-b13e-e712434c7a13`) — 지금 하는 일·다음 할 일·⛔절대 하지 말 것이 여기 있다. 옛 로그를 뒤지지 말 것
3. git pull로 최신 코드 확인
4. 코드 확인 → 실제 구현 상태 파악
5. 사용자에게 현재 상태 요약 + 오늘 작업 방향 확인
> ⚠️ 현재 상태 페이지의 "미완료" 항목이 **이미 끝나 있을 수 있다**(2026-08-21 GA4 등록 건). 손대기 전에 실물을 먼저 확인한다.

### 작업 완료 시
1. 코드 수정 완료
2. git commit & push (script.js/style.css 고쳤으면 배포/캐시 규칙 확인)
3. **완료 기록은 `docs/CHANGELOG.md`의 "작업 일지(날짜별)"에 그날 한 일 한 줄 추가.** CLAUDE.md "현재 상태"는 한두 줄만 갱신 (← CLAUDE.md 비대화 방지)

### 의사결정
- 기획 방향 변경 → 반드시 사용자 확인 후 진행
- 버그 수정, 코드 정리 → Claude가 바로 진행 가능
- 새 기능 추가 → 사용자와 범위 합의 후 진행

### 문서 관리 원칙
- CLAUDE.md = **살아있는 규칙 + 현재 상태(요약) + 미완료 TODO**만. 길이를 짧게 유지(규칙이 묻히지 않게).
- 완료 이력·옛 버전 → `docs/CHANGELOG.md`
- 규칙이 **코드로 강제되면**(예: 캐시 always-fresh) CLAUDE.md엔 핵심 한 줄만, 자세한 건 코드/메모리에

## 작업 기록 규칙 (필수)
- CHANGELOG.md를 업데이트할 때는 반드시 노션에도 동시에 기록한다. **한쪽만 쓰고 다른 쪽을 빠뜨리지 않는다.**
- 컴퓨터의 `docs/CHANGELOG.md`와 노션 양쪽에 같은 내용을 남긴다. 노션에는 **전문용어 없이 쉬운 한국어로** 요약해서 남긴다.
- **어느 페이지에 쓰는가** (2026-08-21 구조 개편 — 위 "노션 4개 페이지 구조" 참고):
  - 그날 한 일 → 📒 **작업 로그 2026-08**(`3c37112a-ae24-8122-85ae-e833111d46d1`)의 **맨 위**에 추가. ⚠️ 달이 바뀌면 **새 페이지를 만든다**
  - 상태가 바뀌면 → 📍 **현재 상태**(`3c37112a-ae24-8167-b13e-e712434c7a13`)의 해당 줄을 **덮어쓴다**(쌓지 않는다). 끝난 건 취소선+✅, 새로 생긴 건 "다음 할 일"에
  - 판단·방침이 바뀌면 → 📖 **결정 기록**(`3c37112a-ae24-8168-9db7-c3b5e6b33204`)에. 작업 로그엔 "왜"를 길게 쓰지 않는다
- ⚠️ **옛 페이지(`39c7112a-...`)에 새로 쓰지 말 것** — 2026-08-20부로 은퇴했고 아무도 안 본다.
